server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: qualquer rota cai no index.html (o React cuida da navegação)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
 